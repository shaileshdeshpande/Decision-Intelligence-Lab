# Resume Library

Store each released resume as a versioned file. `Resume_v1.0.docx` is the initial editable baseline. Create a clearly named role-specific copy before tailoring it to an application.
